#!/bin/zsh
# =============================================================================
# Agent scripts + workspace root
# =============================================================================
# Loaded last (env/ stage) so $HOME/bin keeps highest PATH priority.

export PATH="$HOME/bin:$PATH"
export CODE_ROOT="$HOME/Documents/personal"
